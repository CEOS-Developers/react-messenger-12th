import React, {useState, useEffect } from 'react';
import styled from 'styled-components';

export default function MessageViewer(props) {

    let a = props.messageHtml;
    console.log(props.messageHtml);

    return (
        {}

    );
}